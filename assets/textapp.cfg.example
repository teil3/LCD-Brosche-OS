# TextApp Konfigurationsdatei
# Diese Datei muss als /textapp.cfg auf das LittleFS hochgeladen werden
# LCD-Brosche muss im Transfer-Modus sein.
#python3 tools/upload_system_image.py /dev/ttyACM0 '/home/litwan/src/LCD-brosche/ESP32-D0WD-V3-TFT-1.28V1.1/ESP32-BoardOS/assets/textapp.cfg'  /
#
# Format: KEY=VALUE (eine Einstellung pro Zeile)
# Kommentare beginnen mit #

# Anzeigemodus:
# - single_line   = Eine Zeile zentriert
# - multi_line    = Mehrere Zeilen (nutze | als Zeilenumbruch)
# - big_words     = Einzelne grosse Wörter nacheinander
# - big_letters   = Einzelne grosse Buchstaben nacheinander
MODE=single_line

# Text zum Anzeigen
# Für multi_line Modus: nutze | für Zeilenumbrüche
# Beispiel: TEXT=Zeile 1|Zeile 2|Zeile 3
TEXT=Teil3 GmbH | online 3D-Drucken | mach dein Teil!

# Farbe im RGB565-Format (Hex)
# Beispiele:
#   0xFFFF = Weiss
#   0xF800 = Rot
#   0x07E0 = Grün
#   0x001F = Blau
#   0xFFE0 = Gelb
#   0xF81F = Magenta
#   0x07FF = Cyan
COLOR=0xFFFF
BG_COLOR=0x001F

# Schriftwahl:
# - FONT erwartet den Basisnamen einer `.vlw`-SmoothFont-Datei in `/system/fonts/` (ohne `.vlw`).
#   Beispiel: `FreeSansBold18pt` → `/system/fonts/freesansbold18pt.vlw` (Upload wandelt in Kleinbuchstaben).
# - Vorhandene Beispiele: FreeSans18pt, FreeSansBold18pt, FreeSansBold24pt, FreeSerif18pt.
# - Ohne `FONT` steuert SIZE nur, ob FreeSansBold18pt (SIZE < 4) oder FreeSansBold24pt geladen wird.
# - Fehlt eine Datei, verwendet die App automatisch die kleine Systemschrift.
FONT=FreeSansBold18pt
#SIZE=2

# Geschwindigkeit in Millisekunden (gemeinsamer Wert für Wörter & Buchstaben)
# Kleinere Werte = schneller (10-10000). Keys LETTER_SPEED, WORD_SPEED oder CYCLE_SPEED funktionieren identisch.
LETTER_SPEED=600

# Beispiel-Konfigurationen:
#
# Grosse Wörter:
# MODE=big_words
# TEXT=Teil3 online 3D-Drucken
# COLOR=0x07E0
# FONT=FreeSansBold24pt
# WORD_SPEED=300
#
# Grosse Buchstaben:
# MODE=big_letters
# TEXT=HELLO
# COLOR=0xF800
# FONT=FreeSansBold24pt
# LETTER_SPEED=500
#
# Mehrzeilig:
# MODE=multi_line
# TEXT=Zeile 1|Zeile 2|Zeile 3
# COLOR=0x001F
# FONT=FreeSansBold18pt
#
# Serif-Beispiel:
# MODE=single_line
# TEXT=Serif Demo
# COLOR=0xFFFF
# FONT=FreeSerif18pt  # erwartet `/system/fonts/FreeSerif18pt.vlw`
